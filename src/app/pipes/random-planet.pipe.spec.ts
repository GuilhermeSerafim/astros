import { RandomPlanetPipe } from './random-planet.pipe';

describe('RandomPlanetPipe', () => {
  it('create an instance', () => {
    const pipe = new RandomPlanetPipe();
    expect(pipe).toBeTruthy();
  });
});
